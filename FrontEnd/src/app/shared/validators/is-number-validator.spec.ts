import { IsNumberValidator } from './is-number-validator';

describe('IsNumberValidator', () => {
  it('should create an instance', () => {
    expect(new IsNumberValidator()).toBeTruthy();
  });
});
